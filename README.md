# My__ecnr
This project implements a simple substitution cipher where each English letter is replaced by a unique symbol. It includes two functions:  encode_message() → Converts plain text into encoded symbols.  decode_message() → Converts encoded symbols back into readable text.  It’s a fun way to practice cryptography basics and Python dictionary lookups.

# 🔐 Substitution Cipher (Encode & Decode)

A simple **Python project** that implements a **substitution cipher** for encoding and decoding messages.  
Each English letter is replaced with a unique symbol, making the message look encrypted.

---

## 🚀 Features
- **Encode** plain text into symbols.  
- **Decode** symbols back into readable text.  
- Works directly in the terminal.  
- Uses **custom symbol mapping** defined in a dictionary.

---

## 📂 Project Structure

substitution-cipher/<br>
│
├── cipher.py    
└── README.md      

▶️ Usage Instructions

1. Clone or download this repository:
```bash
git clone https://github.com/your-username/substitution-cipher.git

 ```
2. Navigate into the folder:
```bash
cd substitution-cipher
```
3. Run the program:
```bash
python3 My__ecnr.py
```

## 📖 Example
```text
Enter the Message: HELLO WORLD
Plain: HELLO WORLD
Encrypted: *×)){.±{÷)₹```
```


